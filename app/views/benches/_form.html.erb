<%= form_for(@bench) do |f| %>
  <% if @bench.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bench.errors.count, "error") %> prohibited this bench from being saved:</h2>

      <ul>
      <% @bench.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description, rows: 3, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :num_of_genuine %><br>
    <%= f.number_field :num_of_genuine, class: 'form-control' %>
  </div>
  <div class="gorm-group">
    <%= f.label :num_of_imposter %><br>
    <%= f.number_field :num_of_imposter, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :strategy %><br>
    <%= f.text_field :strategy, class: 'form-control' %>
  </div>

  <%= f.submit "Create Benchmark", class: "btn btn-primary" %>

<% end %>
