<p id="notice"><%= notice %></p>

<h1>Tasks</h1>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Score</th>
      <th>Progress</th>
      <th>Finished</th>
      <th>UUID</th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= link_to task.name, task %></td>
        <td><%= task.score %></td>
        <td><%= task.progress %></td>
        <td><%= task.finished %></td>
        <td><%= task.short_uuid %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<% content_for :sidebar do %>
<%= link_to 'Run Task', new_task_path, class: 'btn btn-primary' %>
<% end %>
