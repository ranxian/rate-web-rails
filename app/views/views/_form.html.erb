<%= form_for(@view) do |f| %>
  <% if @view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@view.errors.count, "error") %> prohibited this view from being saved:</h2>

      <ul>
      <% @view.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :num_of_classes %><br>
    <%= f.text_field :num_of_classes %>
  </div>
  <div class="field">
    <%= f.label :num_of_samples %><br>
    <%= f.text_field :num_of_samples %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
